---
name: fixture-schema-reference
description: Explain the current Cypress incident-fixture schema, valid fields, timelines, and validation commands. Use when the user needs a schema reference rather than fixture generation.
---

# Incident fixture schema reference

Read the canonical schema at `web/cypress/support/incidents_prometheus_query_mocks/schema/fixture-schema.json`. Use [references/schema-reference.md](references/schema-reference.md) as an orientation guide, but resolve any discrepancy in favor of the JSON Schema.

Answer the user's specific schema question. Do not generate or modify a fixture unless they also request it.
