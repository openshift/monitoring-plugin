export enum QueryParams {
  Dashboard = 'dashboard',
  RefreshInterval = 'refreshInterval',
  TimeRange = 'timeRange',
  EndTime = 'endTime',
  Datasource = 'datasource',
  Project = 'project',
  Edit = 'edit',
  Start = 'start',
  Refresh = 'refresh',
}
